#!/usr/bin/python

import os
import sys


### README
# The use of this script is to find the distribution of date over our data set of questions, it takes
# a folder of files (each file is a question site html), and outputs 1 text file of a list of dates and scores

folder =  os.listdir(